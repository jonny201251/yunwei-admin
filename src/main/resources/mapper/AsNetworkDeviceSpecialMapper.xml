<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sss.yunweiadmin.mapper.AsNetworkDeviceSpecialMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.sss.yunweiadmin.model.entity.AsNetworkDeviceSpecial">
        <id column="id" property="id" />
        <result column="as_id" property="asId" />
        <result column="rom" property="rom" />
        <result column="flash" property="flash" />
        <result column="port_total" property="portTotal" />
        <result column="ios" property="ios" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, as_id, rom, flash, port_total, ios
    </sql>

</mapper>
